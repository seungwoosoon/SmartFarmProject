spring:
  datasource:
    url: jdbc:postgresql://db:5432/smartfarm
    username: son
    password: 1234

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  servlet:
    session:
      cookie:
        same-site: Lax
        secure: false